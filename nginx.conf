server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;
    location / { try_files $uri $uri/ /index.html; }
    add_header Content-Security-Policy "default-src 'self'; connect-src api.github.com;" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
}
